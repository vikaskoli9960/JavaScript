let doNotshere = 'this variable is nt shere'

const message = () => {
    const name = " akash ";
    const age = 40;
    return name + ' is ' +
        age + ' years old.';
};

export default message;